TOGETHER_API_KEY=
TOGETHER_MODEL=meta-llama/Llama-3.3-70B-Instruct-Turbo

# Voice processing (see README for setup instructions)
FFMPEG_BIN=ffmpeg
WHISPER_MODEL=base
PIPER_VOICE=models/en_US-lessac-medium.onnx

# Optional: force GPU/CPU (auto-detected if not set). Render has no GPU — leave as false.
USE_CUDA=false
